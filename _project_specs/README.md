This directory tracks the project state, separating it from code.

## Structure

- `/features`: Specifications for features (before coding).
- `/todos`: Atomic tasks (active, completed).
- `/session`: Context for resumability.

See `knowledges/claude_bootstrap.md` for details.
